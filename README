This will contain all config, scripts, and other files required for the project.

ALL SCRIPTS MUST BE RUN FROM THE 'SCRIPTS' DIRECTORY.

TODOS:
<done> Configure the environment
<done> Configure the events
<done> Run the simulation with the digits of your UT EID as the seed
<done> Email Lane your pop files (so we all have them)
<done> Write scripts to run the invasion trials. 

<All> Zip up and send Lane the experimental data files
